# Written fresh by .github/scripts/entry-health.mjs on every run and read straight into the
# step summary / issue body. A committed copy is never regenerated, so it just sits in the repo
# going stale — which is the exact class of bug the count markers exist to kill.
entry-health-report.md
